# PWA Icons

`icon-192.png` and `icon-512.png` are required here for the PWA install prompt to work.

Generate from the OMEN logo. Use [maskable.app](https://maskable.app) to create maskable versions that fill the safe zone correctly on Android.

**Required before PWA install prompt will work.**

Do not commit placeholder PNGs — generate real icons first.
